"""Configuration values for the sim camera."""

# camera positions
camTargetPos = [0, 0, 0]
cameraUp = [0, 0, 1]
cameraPos = [1, 1, 1]

## rotational values
yaw = 40  # rotation around vertical axis
pitch = 10.0 # rotation around side-to-side axis
roll= 0 # rotation around front-to-back axis

# additional params (see API guide!)
camDistance = 2
pixelWidth = 1024
pixelHeight = 768

upAxisIndex = 2
nearPlane = 0.01
farPlane = 100

fov = 50
